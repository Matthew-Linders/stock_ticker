# READ ME
My dad really likes the old board game Stock Ticker.
Let's make it with pygame.
Why?
I want to learn pygame.
And because it's fun :)

## To-Do:
1. Clone the repository:

    ```bash
    git clone https://github.com/Matthew-Linders/stock_ticker.git
    ```

2. Create a virtual environment (optional but recommended):

    ```bash
    cd stock_ticker # Open the repository
    python -m venv venv
    venv\Scripts\activate # On linux/mac: source venv/bin/activate
    ```

3. Install the required packages:

    ```bash
    pip install -r requirements.txt
    ```

## Here's my plan:
- UI elements
    - The board to show the stocks and their values
    - Pieces on the board corresponding to each commodity
    - Dice that show the most recent roll
    - Player cards
        - How much money?
        - How much of each stock?
    - Rounds played
    - Button to end the round
    - Buy/Sell options 

## File organizaiton
- Player cards
- Dice
- Board 
- Stocks
    - One per stock. Tracks value and piece on board. 
- Commerce screen?

We'll figure it out. This is a learning experience.
